<?xml version="1.0" encoding="utf-8" ?>
<forms:BaseContentPage
    xmlns:forms="clr-namespace:Storm.Mvvm.Forms;assembly=Storm.Mvvm.Forms"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
    x:Class="AlbumLieux.MainPage"
    Title="Album">
    <ListView ItemTapped="ListView_ItemTapped"
              ItemsSource="{Binding SpotList}"
              SelectionMode="None">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Horizontal" Margin="3">
                        <ffimageloading:CachedImage
                            Source="{Binding ImageUrl}"
                            LoadingPlaceholder="loading.png"
                            DownsampleToViewSize="True"
                            WidthRequest="48"
                            HeightRequest="48" />
                        <StackLayout Orientation="Vertical" Margin="6, 0, 0, 0">
                            <Label Text="{Binding Name}" TextColor="Black" FontSize="Default"/>
                            <Label Text="{Binding City}" TextColor="#888" FontSize="12"/>
                        </StackLayout>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</forms:BaseContentPage>
